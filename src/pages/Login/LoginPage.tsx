import React from "react";
import LoginStyles from "./LoginStyles";

const LoginPage = () => {
  return (
    <LoginStyles>
      <h1>Login Page</h1>
    </LoginStyles>
  );
};

export default LoginPage;
